import React from "react";
import LoadingCard from "./../../components/ui/skeleton/loading-card";

const Loading = () => {
  return (
    <div>
      <LoadingCard />
    </div>
  );
};

export default Loading;
